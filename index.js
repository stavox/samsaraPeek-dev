export const handler = async (event) => {
  // TODO implement
  
      const response = {
        statusCode: 200,
        body: 'FUNCIONA',
      }
      console.log(JSON.parse(body))

  return response
}
